version: "3.9"

services:
  NODE_NAME:
    build: .
    command: ["--poolId", "4", "--mnemonic", "THE_MNEMONIC", "--initialStake", "THE_STACKE", "--keyfile", "./arweave.json", "--network", "korellia"]
    restart: always
